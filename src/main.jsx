import React from "react";
import ReactDOM from "react-dom/client";
import TheVanApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TheVanApp />
  </React.StrictMode>
);
